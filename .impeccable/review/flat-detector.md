# Detector scope

Ran once on flat-mockup/index.html, styles.css and app.js. DEGRADED: htmlparser2, css-select, css-tree and domutils unavailable; regex fallback does not compute contrast or custom properties.

Remaining warning: border-accent-on-rounded matched the 3px bottom underline of the selected mobile tab; this control is square and uses a conventional selected-tab underline, not an accent on a rounded card.

Font-ramp advisories compare the new flat alternative with the still-spatial root DESIGN.md. The new design explicitly changes typography; the documentation handoff will replace the root design record and retain the spatial record in spatial-mockup/DESIGN.md. These are intentional redesign values, not undocumented drift in the old spatial surface.
