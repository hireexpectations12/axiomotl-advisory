# Release verification 19 September 2026

Production: https://axiomotl-advisory.vercel.app/

Vercel deployment `dpl_EDJEgXpffJtamynpJLsj2aCF315y` is Ready. Database publication version 15 completed at 2026-09-18T14:17:24Z. The previous draft and publication were backed up under workspace `outputs/release-backups`. Existing form configurations and additional pages were preserved by the version-checked save/publish workflow.

Changes: opaque hero cards with green hover glow, removed hero facts and detail strip, stage links to service sections, aligned engagement numbers, purple last-word heading accents, and bottom contact fields. Contact sending is intentionally disabled pending the owner's email setup decision; an email link and an explicit availability notice remain visible.

Checks: 72 unit/database tests passed; production compilation and TypeScript checks passed. Fourteen public combined-page browser tests passed. The editor save/reload test identified headings with nested colour spans being classified as generic components; heading text classification was fixed and the targeted test passed after the final fix. Live DOM checks confirmed contact fields, disabled submission, removed detail strip and lavender hero accent. Live width checks at 320, 768 and 1440px found no page-wide horizontal overflow; local checks also covered 390 and 1024px. Numbers share a horizontal baseline on tablet/desktop and left alignment on mobile. Local mobile hero and contact screenshots were visually inspected.

Training book source is in workspace `outputs/training/manual.md`. It documents the held contact-delivery status and distinguishes guided-tool email drafts from actual submission delivery.

## Backend settings expansion

Production deployment `dpl_8VruFYvFem2txtZbj44SUgA3QYDc` is Ready at the existing production alias. This was a code release; it did not republish or replace the current page draft/publication.

Added grouped business/branding/search settings, expiry-based announcements, shared mobile/desktop menu controls, additional footer text/links, held contact-delivery preparation, staff roles, private manual enquiries, internal-link/form status checks and revision change summaries. Site editors cannot publish/restore; only original owners can manage staff. No email was sent or delivery configured.

Verification: 83 automated unit/database/API tests passed; TypeScript and production build passed. Four targeted dashboard workflows passed, including settings persistence, editor restrictions, enquiry updates and phone layout. Screenshots were inspected. Production `/` and `/admin` return 200; unauthenticated enquiries/staff/status APIs return 401. Real private-bucket read/write verification passed, anonymous download was denied, and the temporary verification object was removed. Private enquiry storage is separate from all public site documents and media.

## Layout and GSAP alignment

Production deployment dpl_5ifFhkrznoWx29ZBiFRtYCDfhyuE is Ready. Targeted layout publication version 17 completed at 2026-09-18T16:19:36Z. Existing HTML, settings, forms and pages were preserved, with the previous document backed up under outputs/release-backups.

Aligned page gutters, section spacing, workflow cards, engagement steps and diagram panels; corrected tablet navigation and kept GSAP hover geometry stable. TypeScript and production build passed, as did 16 combined-page browser tests and the additional GSAP regression test. Live checks at 320, 390, 768, 1024 and 1440px confirmed equal card heights, aligned hero/content margins and no horizontal overflow. Email delivery and public contact submission remain on hold.

