# Combined site verification — 17 September 2026

Approved scope: purple/teal photographic visual identity from the supplied ChatGPT site, existing Vercel service finder and decision brief, consolidated content and compatibility with the owner editor.

Review URL: http://localhost:3000/design-preview (local production server).

Passed:

- `npm test`: 72 tests across 6 files.
- `npx playwright test`: 20 browser tests on the production build, including 9 combined-site checks and the existing editor, public-site and security checks.
- `npm run typecheck` and `npm run format:check`.
- `npm run build`: successful production compilation and route generation.
- Frontend Design Premium strict static audit: zero findings, recorded in `combined-premium-audit.json`.
- `git diff --check`.

Browser checks cover local asset loads, anchors, disclosures, all four service recommendations, embedded preference override, Back, Escape and opener focus, empty-answer validation, decision summary download/reset, responsive layouts at 320/390/768/1440px, reduced motion, and editing/saving/reloading the combined document in the owner workspace. Owner mutations are exercised with isolated mocked API state, not the production database.

Desktop, mobile, mobile hero and mobile dialog screenshots were inspected and saved in `../../outputs/combined-site/`. The visual review confirmed a consistent purple/teal palette, readable photographic opening, consolidated services, working-sheet decision brief and usable mobile dialog.

Integration fixes: optional sculpture animation now skips absent elements; the combined stylesheet defines fonts for the editor canvas; sandbox previews embed nested font assets and deployment tracing includes them.

Release boundary: neither live website nor the owner's database draft/publication was replaced. The reviewed editable artifact is `src/generated/combined.json`, regenerated by `npm run design:build`. Publication should use the existing versioned draft/publish workflow after review, preserving any newer owner changes. The original image licensing/provenance records were not supplied; existing public assets were reused and their source is documented in `design/combined/README.md`.

## Three.js hero update

The latest owner reference, `example2.mp4`, supersedes the photographic hero. The opening now uses a dark angled circuit board with five supplied icon buttons: Analyze, Design, Decide, Transition and Sustain. The confirmed Vercel axolotl logo remains.

Verified on the local production build:

- 72 unit tests passed; TypeScript and production compilation passed.
- All 13 combined-site browser checks passed with `npx playwright test tests/e2e/combined.spec.ts --trace=off --reporter=line`.
- Tests cover the five keyboard-selectable stages and mapped service links, animated/pause/resume states, reduced-motion mobile controls, WebGL fallback, all existing form flows and mocked owner editing.
- Desktop and mobile screenshots inspected: `../../outputs/hero-reference/desktop.png` and `mobile.png`. No page errors were observed in the initial visual capture.
- Formatting check passed. Functional tests use reduced motion; the dedicated WebGL test explicitly enables animation. Repeated GPU readback during traced animation caused intermittent software-renderer stalls, so this verification run disabled tracing.

The production server serves the updated preview at http://localhost:3000/design-preview. No live deployment or database publication was performed.

## Exact artwork and GSAP update

The latest 1258×712 supplied artwork replaces the previous Three.js desktop rendering. It is served unchanged with proportional native navigation, CTA and stage controls. GSAP supplies moving signal lights and hover feedback. Desktop text within the artwork is not independently editable; mobile and semantic copy remain HTML.

Verification: desktop screenshot at the reference dimensions with reduced motion compared against the source PNG with 100% identical RGB channels (zero mean channel difference). Desktop and mobile screenshots were inspected in `outputs/hero-reference/exact-desktop.png` and `exact-mobile.png`. Thirteen combined browser checks passed in the main run; the remaining navigation check passed after narrowing its locator to the desktop navigation. TypeScript, production build and formatting checks passed. The production preview was restarted on port 3000. No live publication occurred.

## Sharp native hero and animated atmosphere

The blurred image-backed hero was replaced with native HTML typography/controls and scalable CSS/SVG scenery. Side statements are pure white. Original local logo/icon assets are displayed individually. GSAP now animates three luminous arc highlights and two purple glow layers alongside the connection signals; text stays stationary.

All 14 combined browser tests passed, including native heading visibility, white side statements, actual arc/glow movement and pause, forms, stage links, mobile layouts and owner editing. TypeScript, formatting and production build passed. Desktop/mobile screenshots were inspected, and the production preview restarted on port 3000. The earlier exact-pixel bitmap result is historical and does not describe this native version. No live publication was performed.
